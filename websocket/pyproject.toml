[project]
name = "chat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncio>=3.4.3",
    "fastapi[standard]>=0.115.6",
    "websockets>=14.1",
]

[dependency-groups]
dev = [
    "ruff>=0.9.1",
]
